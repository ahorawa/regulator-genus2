_<x> := PolynomialRing(Rationals());

for a in [-20 .. 20] do
  g := x^4 + a;
  C := HyperellipticCurve(x*(x-1)*g + 1);
  pts := [C![0,1,1], C![1,1,1]];

  J := Jacobian(C);
  oo := C![1,1,0];    

  for P in pts do
    Order(J![P, oo]);
  end for;
  
  print("---");
end for;